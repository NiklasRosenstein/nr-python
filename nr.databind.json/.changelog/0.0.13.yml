release_date: '2020-07-02'
changes:
- type: change
  component: general
  description: The `MultiType` serializer now (again) catches only `SerializationTypeError`,
    but the `Struct` serializer now raises a `SerializationTypeError` instead of `SerializationValueError`
    if a field is missing
  fixes: []
