changes:
- type: fix
  component: general
  description: '`nr.proxy` functions that accept a proxy object now accept the proxy
    type or directly the type itself to accomodate for use cases where the proxy is
    cast to the wrapped type'
